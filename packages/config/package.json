{
  "name": "@riki/config",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Layered config resolution and validation. The only place that reads process.env",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --build"
  }
}
