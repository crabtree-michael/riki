[package]
name = "riki-vision"
description = "The sidecar binary: supervisor-friendly, stdio protocol"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
riki-capture = { workspace = true }
riki-ipc = { workspace = true }

[dev-dependencies]
# The integration test spawns the real binary and speaks the real protocol to it, which means it
# has to build the same messages the app would.
serde_json = { workspace = true }
