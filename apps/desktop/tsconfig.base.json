{
  "$schema": "https://json.schemastore.org/tsconfig",
  // Options common to the app's four projects. Kept separate from tsconfig.json so the solution
  // file stays a list of references and nothing else.
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist"
  }
}
