/**
 * Electron main process: app lifecycle, tray, global hotkeys, window management, the sidecar
 * supervisor, and the IPC host.
 *
 * Keep this thin. Business logic belongs in packages/ where it is testable without a window
 * (REPO_SKELETON.md §2.1).
 *
 * Skeleton only — no implementation yet.
 */

export {};
