/**
 * gsi-replay — Replay a recorded fixture session into a running dev build. What `pnpm dev:replay` drives.
 *
 * Dev-only. Nothing in tools/ ships.
 *
 * Skeleton only — no implementation yet.
 */

export {};
