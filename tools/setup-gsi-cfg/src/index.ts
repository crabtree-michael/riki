/**
 * setup-gsi-cfg — Write the gamestate_integration cfg and generate the per-install GSI token.
 *
 * Dev-only. Nothing in tools/ ships.
 *
 * Skeleton only — no implementation yet.
 */

export {};
