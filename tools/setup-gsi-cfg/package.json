{
  "name": "@riki-tools/setup-gsi-cfg",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Write the gamestate_integration cfg and generate the per-install GSI token.",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --build"
  }
}
